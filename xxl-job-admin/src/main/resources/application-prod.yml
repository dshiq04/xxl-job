xxl-job:
  database:
    type: postgresql
    host: ${DB_HOST}
    port: ${DB_PORT}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    database: ${DB_DATABASE}
    driver-class-name: org.postgresql.Driver